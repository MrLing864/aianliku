import { config } from "dotenv";
async function main() { config({ path: ".env.local", quiet: true }); const [{ demoCases }, { getDb, ensureIndexes, isDbConfigured }] = await Promise.all([import("../src/data/demo-cases"), import("../src/lib/db/cloudbase")]); if (!isDbConfigured()) throw new Error("CLOUDBASE_ENV / CLOUDBASE_SECRET_ID / CLOUDBASE_SECRET_KEY 未在 .env.local 配置"); await ensureIndexes(); const db = await getDb(); for (const item of demoCases) await db.collection("cases").updateOne({ id: item.id }, { $set: item }, { upsert: true }); console.log(`Seeded or updated ${demoCases.length} clearly-labeled demo cases.`); process.exit(0); }
void main();
