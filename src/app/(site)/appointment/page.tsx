import type { Metadata } from "next";
import { CalendarCheck2, ShieldCheck, UserRoundCheck } from "lucide-react";
import { AppointmentForm } from "@/components/appointment-form";
import { Card, CardContent } from "@/components/ui/card";
export const metadata: Metadata = { title: "预约企业 AI 专家", robots: { index: false, follow: false } };
export default async function AppointmentPage({ searchParams }: { searchParams: Promise<{ report?: string }> }) { const reportId = (await searchParams).report; return <main className="container-page py-14 lg:py-20"><div className="grid gap-10 lg:grid-cols-[.75fr_1.25fr] lg:gap-14"><div><p className="text-xs font-semibold tracking-[0.16em] text-primary">人工回访</p><h1 className="mt-4 text-4xl font-semibold tracking-[-0.05em] sm:text-5xl">预约企业 AI 专家</h1><p className="mt-5 text-sm leading-7 text-muted-foreground">适合已经看过体检报告，希望进一步确认第一阶段范围、数据准备和实施路径的企业。</p><div className="mt-9 space-y-5">{[{ icon: UserRoundCheck, text: "平台人工阅读需求后回访" }, { icon: CalendarCheck2, text: "通常 2 个工作日内联系" }, { icon: ShieldCheck, text: "不承诺未经验证的 ROI 与效果" }].map(({ icon: Icon, text }) => <div key={text} className="flex items-center gap-3 text-sm"><span className="grid size-9 place-items-center rounded-xl border bg-card text-primary"><Icon className="size-4" /></span>{text}</div>)}</div></div><Card className="shadow-none"><CardContent className="p-6 sm:p-8"><h2 className="mb-6 text-xl font-semibold">预约信息</h2><AppointmentForm reportId={reportId} /></CardContent></Card></div></main>; }
