import { CatalogIcon } from "@/components/icon-map";
import { Badge } from "@/components/ui/badge";
import { Card, CardContent } from "@/components/ui/card";
import { industries, scenarios } from "@/lib/catalog";
export default function TaxonomiesPage() { return <div><p className="text-xs font-semibold text-primary">内容治理</p><h1 className="mt-2 text-3xl font-semibold tracking-tight">分类与场景词表</h1><p className="mt-2 max-w-2xl text-sm leading-6 text-muted-foreground">AI 只能映射到以下正式标签，不能自行创建新标签。行业分类表带版本号，便于未来标准迁移。</p><div className="mt-7 grid gap-6 xl:grid-cols-2"><Card className="shadow-none"><CardContent className="p-6"><div className="flex items-center justify-between"><h2 className="text-lg font-semibold">行业分类</h2><Badge>GB/T 4754-2017 + 第1号修改单</Badge></div><div className="mt-5 divide-y">{industries.map((item) => <div key={item.id} className="flex items-center gap-3 py-4"><span className="grid size-9 place-items-center rounded-lg border text-primary"><CatalogIcon name={item.icon} className="size-4" /></span><div className="min-w-0 flex-1"><p className="text-sm font-medium">{item.displayName}</p><p className="mt-1 text-xs text-muted-foreground">代码 {item.code} · {item.slug}</p></div>{item.featured && <Badge variant="secondary">前台突出</Badge>}</div>)}</div></CardContent></Card><Card className="shadow-none"><CardContent className="p-6"><div className="flex items-center justify-between"><h2 className="text-lg font-semibold">AI 场景</h2><Badge variant="outline">受控词表 V1</Badge></div><div className="mt-5 divide-y">{scenarios.map((item) => <div key={item.id} className="py-4"><div className="flex items-center gap-3"><span className="grid size-9 place-items-center rounded-lg border text-primary"><CatalogIcon name={item.icon} className="size-4" /></span><div className="min-w-0 flex-1"><p className="text-sm font-medium">{item.name}</p><p className="mt-1 text-xs text-muted-foreground">{item.slug}</p></div>{item.featured && <Badge variant="secondary">前台突出</Badge>}</div><p className="ml-12 mt-2 text-xs leading-5 text-muted-foreground">同义词：{item.synonyms.join("、")}</p></div>)}</div></CardContent></Card></div></div>; }
