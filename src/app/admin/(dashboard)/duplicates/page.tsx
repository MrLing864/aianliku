import { Fingerprint } from "lucide-react";
import { DuplicateReviewCard } from "@/components/duplicate-review-card";
import { listDuplicateCandidates } from "@/lib/repositories/admin";
export default async function DuplicatesPage() { const items = await listDuplicateCandidates(); return <div className="mx-auto max-w-5xl"><p className="text-xs font-semibold text-primary">内容治理</p><h1 className="mt-2 text-3xl font-semibold tracking-tight">重复候选审核</h1><p className="mt-2 max-w-2xl text-sm leading-6 text-muted-foreground">综合分 ≥ 0.90 阻止直接发布，0.75–0.90 给出复核提示。除完全相同来源外，系统不会自动合并案例。</p>{items.length ? <div className="mt-7 space-y-4">{items.map((item) => <DuplicateReviewCard key={item.id} item={item} />)}</div> : <div className="mt-8 grid min-h-72 place-items-center rounded-2xl border border-dashed text-center"><div><Fingerprint className="mx-auto size-9 text-muted-foreground" /><h2 className="mt-4 font-semibold">没有待处理的重复候选</h2><p className="mt-2 text-sm text-muted-foreground">批量导入发现中高相似记录后会出现在这里。</p></div></div>}</div>; }
