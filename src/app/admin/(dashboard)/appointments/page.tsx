import { CalendarClock, Mail, MessageCircle, Phone } from "lucide-react";
import { AppointmentStatus } from "@/components/appointment-status";
import { Badge } from "@/components/ui/badge";
import { Card, CardContent } from "@/components/ui/card";
import { listAppointments } from "@/lib/repositories/admin";
export default async function AppointmentsPage() { const items = await listAppointments(); return <div><p className="text-xs font-semibold text-primary">线索管理</p><h1 className="mt-2 text-3xl font-semibold tracking-tight">专家预约</h1><p className="mt-2 text-sm text-muted-foreground">按提交时间倒序，人工回访后及时更新状态。</p>{items.length ? <div className="mt-7 grid gap-4 xl:grid-cols-2">{items.map((item) => <Card key={item.id} className="shadow-none"><CardContent className="p-6"><div className="flex items-start justify-between gap-4"><div><div className="flex items-center gap-2"><h2 className="font-semibold">{item.name} · {item.company}</h2>{item.role && <Badge variant="secondary">{item.role}</Badge>}</div><p className="mt-2 text-xs text-muted-foreground">{new Intl.DateTimeFormat("zh-CN", { dateStyle: "medium", timeStyle: "short" }).format(new Date(item.createdAt))}</p></div><AppointmentStatus id={item.id} value={item.status} /></div><p className="mt-5 rounded-lg bg-muted/55 p-4 text-sm leading-7">{item.need}</p><div className="mt-4 flex flex-wrap gap-4 text-xs text-muted-foreground">{item.phone && <span className="flex items-center gap-1.5"><Phone className="size-3.5" />{item.phone}</span>}{item.wechat && <span className="flex items-center gap-1.5"><MessageCircle className="size-3.5" />{item.wechat}</span>}{item.preferredTime && <span className="flex items-center gap-1.5"><CalendarClock className="size-3.5" />{item.preferredTime}</span>}{item.reportId && <span className="flex items-center gap-1.5"><Mail className="size-3.5" />关联报告 {item.reportId}</span>}</div></CardContent></Card>)}</div> : <div className="mt-8 grid min-h-72 place-items-center rounded-2xl border border-dashed text-center"><div><CalendarClock className="mx-auto size-9 text-muted-foreground" /><h2 className="mt-4 font-semibold">暂无预约</h2><p className="mt-2 text-sm text-muted-foreground">用户从完整体检报告提交的预约会出现在这里。</p></div></div>}</div>; }
