import { notFound } from "next/navigation";
import { CaseEditor } from "@/components/case-editor";
import { CaseMergePanel } from "@/components/case-merge-panel";
import { isMongoConfigured } from "@/lib/db/mongodb";
import { getAdminCase, listAdminCases } from "@/lib/repositories/admin";
export default async function EditCasePage({ params }: { params: Promise<{ id: string }> }) { const item = await getAdminCase((await params).id); if (!item) notFound(); const mongoConfigured = isMongoConfigured(); const targets = mongoConfigured ? (await listAdminCases(500)).filter((entry) => entry.id !== item.id && entry.contentStatus === "published").map((entry) => ({ id: entry.id, title: entry.title, organization: entry.organization.name })) : []; return <div className="mx-auto max-w-5xl"><p className="text-xs font-semibold text-primary">案例管理</p><h1 className="mt-2 text-3xl font-semibold tracking-tight">编辑案例</h1><p className="mt-2 text-sm text-muted-foreground">所有发布动作都会记录审计日志。</p><div className="mt-7"><CaseEditor item={item} readOnlyDemo={!mongoConfigured} /></div>{mongoConfigured && item.contentStatus !== "merged" && item.contentStatus !== "deleted" && targets.length > 0 && <div className="mt-7"><CaseMergePanel sourceId={item.id} targets={targets} /></div>}</div>; }
