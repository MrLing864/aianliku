import type { MetadataRoute } from "next";
import { industries, scenarios } from "@/lib/catalog";
import { env } from "@/lib/env";
import { listCases, listCaseSitemapEntries } from "@/lib/repositories/cases";
export default async function sitemap(): Promise<MetadataRoute.Sitemap> { const cases = await listCaseSitemapEntries(); const base = env.NEXT_PUBLIC_SITE_URL; const staticPages = ["", "/cases", "/about", "/contact", "/privacy", "/terms"].map((path) => ({ url: `${base}${path}`, lastModified: new Date(), changeFrequency: path === "/cases" ? "daily" as const : "monthly" as const, priority: path === "" ? 1 : .7 })); const casePages = cases.map((item) => ({ url: `${base}/cases/${item.slug}`, lastModified: new Date(item.updatedAt), changeFrequency: "monthly" as const, priority: .8 })); const eligibleIndustries = await Promise.all(industries.map(async (entry) => ({ entry, total: (await listCases({ industry: entry.slug, limit: 1 })).total }))); const eligibleScenarios = await Promise.all(scenarios.map(async (entry) => ({ entry, total: (await listCases({ scenario: entry.slug, limit: 1 })).total }))); const catalogs = [...eligibleIndustries.filter((item) => item.total >= 5).map((item) => `/industries/${item.entry.slug}`), ...eligibleScenarios.filter((item) => item.total >= 5).map((item) => `/scenarios/${item.entry.slug}`)].map((path) => ({ url: `${base}${path}`, lastModified: new Date(), changeFrequency: "weekly" as const, priority: .65 })); return [...staticPages, ...casePages, ...catalogs]; }
